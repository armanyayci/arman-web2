@model IEnumerable<Comment>

@*@foreach (var item in Model)
{
            <div>
                <h4>@item.name</h4>

                <div class="row">

                    <div class="col b-5">   
                        <p>@item.message</p>
                    </div>

                </div>

            </div>
}

*@


<section id="section-form" class="light-text">

        <div class="comment-heading">
        <p>Comments</p>
        </div>

        <div class="user-img-box">
            <i class='user-img bx bxs-face' ></i>


        <div id="newcommentprepend">
                @foreach(var item in Model)
                {
                <div class="user-comment-box">
                    <div class="user-img-box">
                        <i class='user-img-com bx bxs-face' ></i>
                    </div>
                <div class="user-comment">
                    <div class="comm-date"><h4>@item.name</h4> <h6>ZAMAN EKLENECEK</h6> </div>
                    <p>@item.message</p>
                </div>
                </div>
                }
        </div>
    </div>

    </section>